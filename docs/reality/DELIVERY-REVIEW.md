# Delivery review — 24 September 2026

Independent source and evidence audit of the working checkout on `codex/reality-debugger-tonga`. No browser or test suite was rerun for this review. Visual acceptance below is the UX reviewer's recorded inspection, not a second visual inspection.

## Supported delivery claims

- The implementation mounts in the existing Cesium application and supplies deterministic time, evidence lenses, cable navigation, source inspection and an interruptible walkthrough without requiring AI credentials.
- Preserved reports record 24 passing checks in the normal-motion run, including a 92.195-second walkthrough. The broad recapture preserves its pointer-test failure; seven focused checks establish actual canvas interruption after the harness correction. The final outage capture has three passing checks. These are separate runs, not one clean final comprehensive run.
- `UX-VALIDATION.md` records opening and accepting all nine desktop and both mobile captures after corrections. Machine reports deliberately retain `visualAcceptance: PENDING`; they are not visual-review verdicts. The human-readable review is the acceptance record.
- Source data distinguishes reported events, qualitative observations, approximate anchors, current cable reference and unresolved fault geography. Recovery does not establish full domestic restoration. TeleGeography attribution and CC BY-NC-SA 3.0 restrictions remain distinct from MIT code; Natural Earth reference geography is separately attributed. No third-party testimony/satellite image reuse is claimed.
- `vercel.json` configures a static Vite build. Mission records and geographic assets are bundled. This supports static deployment preparation, not a claim that a hosted Vercel instance exists or that optional provider/voice services have production backends.

## Findings and remediation

1. **Annotation action:** the initial action only opened inspection. Remediated with source-backed pins at declared geographic anchors, visible source-panel controls, filtering, removal and reset/exit cleanup. Three annotation resolver tests pass. Final production browser checks exercise pinning, visibility, removal and reset successfully.
2. **Relationship operation:** the initial adapter ignored arguments and used fixed endpoints. Remediated with immutable runtime traversal of existing filtered relationships, preserving time, evidence lens, hidden state, edge direction/type and source IDs. Twelve runtime tests pass. World connectors now derive from visible relationships and open their evidence. Production browser checks pass for tracing, filtered rejection and comparison.
3. **Native tracking:** the first run had 104 passes and five failures. Correcting the development server's network execution context resolved all five: the full rerun passed **109/109**, with real terrain requests and model assertions. Bundled imagery substituted 18 map tiles only. The failed run remains preserved; no unverified baseline equivalence is claimed.
4. **Production visual evidence:** earlier screenshot runs used the development server. The final plain-static build run passed 35/35 browser checks and captured nine required desktop states, two mobile states, relationship tracing and a world pin. Its report records URL, serving mode, revision and working-tree changes. The UX record separately documents visual inspection; machine reports alone do not declare visual acceptance. Public deployment remains unperformed.

## Documentation reconciliation

The build journal is reconciled with completed checks. Intermediate failures remain preserved. The architecture review's historical statement that no `vercel.json` existed is superseded by its addendum. Optional voice wiring is tested, but a credentialed live voice session and public hosting are not claimed. Static optional service requests produced 404/405 console messages; the mission completed all checks without unhandled application exceptions.
